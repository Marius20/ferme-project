{% extends 'base.html.twig' %}

{% block title %}{{ sousTypeName }} ({{ typeName }}) - Gestion du Bétail - {{ parent() }}{% endblock %}

{% block page_title %}{{ sousTypeName }}{% endblock %}
{% block page_subtitle %}Gestion complète du cycle de vie - {{ typeName }}{% endblock %}

{% block header_actions %}
    <div class="flex space-x-3">
        <button class="btn btn-primary" onclick="addNewAnimal('{{ type }}', '{{ sousType }}')">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
            Nouveau {{ sousTypeName }}
        </button>
        <button class="btn btn-outline" onclick="importAnimals()">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M9 19l3 3m0 0l3-3m-3 3V10"></path>
            </svg>
            Importer
        </button>
        <button class="btn btn-outline">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
            Exporter
        </button>
    </div>
{% endblock %}

{% block body %}
    <!-- Navigation -->
    <div class="mb-6">
        <nav class="flex" aria-label="Breadcrumb">
            <ol class="flex items-center space-x-4">
                <li>
                    <div>
                        <a href="{{ path('betail_index', {fermeId: fermeActive.id}) }}" class="text-gray-400 hover:text-gray-500">
                            <svg class="w-5 h-5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                                <path d="M10.707 2.293a1 1 0 00-1.414 0l-7 7a1 1 0 001.414 1.414L4 10.414V17a1 1 0 001 1h2a1 1 0 001-1v-2a1 1 0 011-1h2a1 1 0 011 1v2a1 1 0 001 1h2a1 1 0 001-1v-6.586l.293.293a1 1 0 001.414-1.414l-7-7z"></path>
                            </svg>
                            <span class="sr-only">Accueil</span>
                        </a>
                    </div>
                </li>
                <li>
                    <div class="flex items-center">
                        <svg class="w-5 h-5 flex-shrink-0 text-gray-300" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"></path>
                        </svg>
                        <a href="{{ path('betail_famille', {fermeId: fermeActive.id, type: type}) }}" class="ml-4 text-sm font-medium text-gray-500 hover:text-gray-700">{{ typeName }}</a>
                    </div>
                </li>
                <li>
                    <div class="flex items-center">
                        <svg class="w-5 h-5 flex-shrink-0 text-gray-300" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"></path>
                        </svg>
                        <span class="ml-4 text-sm font-medium text-gray-900">{{ sousTypeName }}</span>
                    </div>
                </li>
            </ol>
        </nav>
    </div>

    <!-- Statistiques du type -->
    <div class="grid grid-cols-1 md:grid-cols-5 gap-6 mb-8">
        {% set totalType = betails|length %}
        {% set malesCount = betails|filter(b => b.sexe == 'male')|length %}
        {% set femellesCount = betails|filter(b => b.sexe == 'femelle')|length %}
        {% set nesLaFerme = betails|filter(b => b.isNeDansLaFerme())|length %}
        
        <div class="stat-card">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 rounded-full flex items-center justify-center text-2xl 
                        {% if type == 'bovin' %}bg-purple-100 text-purple-600
                        {% elseif type == 'ovin' %}bg-yellow-100 text-yellow-600  
                        {% elseif type == 'caprin' %}bg-green-100 text-green-600
                        {% elseif type == 'porcin' %}bg-pink-100 text-pink-600
                        {% elseif type == 'equin' %}bg-blue-100 text-blue-600
                        {% else %}bg-gray-100 text-gray-600{% endif %}">
                        {% if type == 'bovin' %}🐄
                        {% elseif type == 'ovin' %}🐑
                        {% elseif type == 'caprin' %}🐐
                        {% elseif type == 'porcin' %}🐷
                        {% elseif type == 'equin' %}🐎
                        {% else %}🐾{% endif %}
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-500">Total</p>
                    <p class="text-3xl font-bold 
                        {% if type == 'bovin' %}text-purple-600
                        {% elseif type == 'ovin' %}text-yellow-600  
                        {% elseif type == 'caprin' %}text-green-600
                        {% elseif type == 'porcin' %}text-pink-600
                        {% elseif type == 'equin' %}text-blue-600
                        {% else %}text-gray-600{% endif %}">{{ totalType }}</p>
                    <p class="text-xs text-gray-500">{{ sousTypeName|lower }}s</p>
                </div>
            </div>
        </div>

        <div class="stat-card">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
                        <svg class="w-7 h-7 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                        </svg>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-500">Mâles</p>
                    <p class="text-3xl font-bold text-blue-600">{{ malesCount }}</p>
                    <p class="text-xs text-gray-500">{{ totalType > 0 ? ((malesCount/totalType*100)|round(1)) : 0 }}%</p>
                </div>
            </div>
        </div>

        <div class="stat-card">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-pink-100 rounded-lg flex items-center justify-center">
                        <svg class="w-7 h-7 text-pink-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                        </svg>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-500">Femelles</p>
                    <p class="text-3xl font-bold text-pink-600">{{ femellesCount }}</p>
                    <p class="text-xs text-gray-500">{{ totalType > 0 ? ((femellesCount/totalType*100)|round(1)) : 0 }}%</p>
                </div>
            </div>
        </div>

        <div class="stat-card">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center">
                        <svg class="w-7 h-7 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-500">Nés sur place</p>
                    <p class="text-3xl font-bold text-green-600">{{ nesLaFerme }}</p>
                    <p class="text-xs text-gray-500">{{ totalType > 0 ? ((nesLaFerme/totalType*100)|round(1)) : 0 }}%</p>
                </div>
            </div>
        </div>

        <div class="stat-card">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-orange-100 rounded-lg flex items-center justify-center">
                        <svg class="w-7 h-7 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                        </svg>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-500">Poids moyen</p>
                    {% set poidsTotal = 0 %}
                    {% set comptePoids = 0 %}
                    {% for betail in betails %}
                        {% if betail.poidsEntree %}
                            {% set poidsTotal = poidsTotal + betail.poidsEntree %}
                            {% set comptePoids = comptePoids + 1 %}
                        {% endif %}
                    {% endfor %}
                    <p class="text-3xl font-bold text-orange-600">{{ comptePoids > 0 ? (poidsTotal / comptePoids)|round(0) : 0 }}</p>
                    <p class="text-xs text-gray-500">kg</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Filtres et Actions -->
    <div class="card mb-6">
        <div class="p-6">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between space-y-4 md:space-y-0">
                <div>
                    <h3 class="text-lg font-semibold text-gray-900">Gestion des {{ sousTypeName }}s</h3>
                    <p class="text-sm text-gray-500">Cycle de vie complet : entrées, reproduction, santé, alimentation, sorties</p>
                </div>
                <div class="flex items-center space-x-3">
                    <div class="flex items-center space-x-2">
                        <label for="view-mode" class="text-sm font-medium text-gray-700">Affichage:</label>
                        <select id="view-mode" class="form-select" onchange="changeViewMode()">
                            <option value="grid">Grille</option>
                            <option value="table" selected>Tableau</option>
                            <option value="card">Cartes</option>
                        </select>
                    </div>
                    <div class="flex items-center space-x-2">
                        <label for="filter-status" class="text-sm font-medium text-gray-700">Statut:</label>
                        <select id="filter-status" class="form-select" onchange="filterAnimals()">
                            <option value="">Tous</option>
                            <option value="actif" selected>Actifs</option>
                            <option value="vendu">Vendus</option>
                            <option value="mort">Morts</option>
                        </select>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Vue tableau des animaux -->
    <div id="table-view" class="card">
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            <input type="checkbox" class="form-checkbox" onchange="toggleSelectAll()">
                        </th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Animal</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Âge/Sexe</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Localisation</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Poids</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Parents</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Santé</th>
                        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    {% for betail in betails %}
                        <tr class="hover:bg-gray-50 animal-row" data-status="{{ betail.isActive ? 'actif' : (betail.typeSortie ?? 'inconnu') }}">
                            <td class="px-6 py-4 whitespace-nowrap">
                                <input type="checkbox" class="form-checkbox animal-checkbox" value="{{ betail.id }}">
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <div class="flex items-center">
                                    <div class="flex-shrink-0 h-10 w-10">
                                        {% if betail.photo %}
                                            <img class="h-10 w-10 rounded-full object-cover" src="{{ betail.photo }}" alt="">
                                        {% else %}
                                            <div class="h-10 w-10 rounded-full bg-gray-200 flex items-center justify-center text-lg">
                                                {% if type == 'bovin' %}🐄
                                                {% elseif type == 'ovin' %}🐑
                                                {% elseif type == 'caprin' %}🐐
                                                {% elseif type == 'porcin' %}🐷
                                                {% elseif type == 'equin' %}🐎
                                                {% else %}🐾{% endif %}
                                            </div>
                                        {% endif %}
                                    </div>
                                    <div class="ml-4">
                                        <div class="text-sm font-medium text-gray-900 flex items-center">
                                            {{ betail.numeroIdentification }}
                                            {% if betail.isNeDansLaFerme() %}
                                                <span class="ml-2 inline-flex px-2 py-0.5 text-xs font-medium rounded-full bg-green-100 text-green-800" title="Né dans la ferme">🏠</span>
                                            {% endif %}
                                        </div>
                                        <div class="text-sm text-gray-500">
                                            {{ betail.race ?: 'Race non spécifiée' }}
                                        </div>
                                    </div>
                                </div>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <div class="text-sm text-gray-900">
                                    {% if betail.age %}
                                        {{ betail.age }} 
                                        {% if betail.age <= 12 %}mois
                                        {% else %}{{ (betail.age/12)|round(1) }} ans
                                        {% endif %}
                                    {% else %}
                                        Age non renseigné
                                    {% endif %}
                                </div>
                                <div class="text-sm">
                                    <span class="inline-flex px-2 py-0.5 text-xs font-medium rounded-full 
                                        {% if betail.sexe == 'male' %}bg-blue-100 text-blue-800
                                        {% else %}bg-pink-100 text-pink-800{% endif %}">
                                        {{ betail.sexe|title }}
                                    </span>
                                </div>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                {% if betail.zone %}
                                    <div class="text-sm text-gray-900">{{ betail.zone.batiment.nom }}</div>
                                    <div class="text-xs text-gray-500">Zone {{ betail.zone.nom }}</div>
                                {% else %}
                                    <span class="text-gray-400">Non assigné</span>
                                {% endif %}
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm">
                                {% if betail.poidsEntree %}
                                    <div class="text-gray-900">{{ betail.poidsEntree|number_format(1, ',', ' ') }} kg</div>
                                    {% if betail.gainPoids %}
                                        <div class="text-xs {{ betail.gainPoids > 0 ? 'text-green-600' : 'text-red-600' }}">
                                            {{ betail.gainPoids > 0 ? '+' : '' }}{{ betail.gainPoids|number_format(1) }} kg
                                        </div>
                                    {% endif %}
                                {% else %}
                                    <span class="text-gray-400">Non pesé</span>
                                {% endif %}
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm">
                                {% if betail.mere or betail.pere %}
                                    <div class="space-y-1">
                                        {% if betail.mere %}
                                            <div class="text-xs text-gray-600">
                                                <span class="font-medium">Mère:</span> {{ betail.mere.numeroIdentification }}
                                            </div>
                                        {% endif %}
                                        {% if betail.pere %}
                                            <div class="text-xs text-gray-600">
                                                <span class="font-medium">Père:</span> {{ betail.pere.numeroIdentification }}
                                            </div>
                                        {% endif %}
                                    </div>
                                {% else %}
                                    <span class="text-gray-400 text-xs">Parents inconnus</span>
                                {% endif %}
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <div class="flex items-center">
                                    {% set dernierSoin = betail.soinsVeterinaires.last %}
                                    {% if dernierSoin %}
                                        {% set joursDepuisSoin = date().diff(dernierSoin.dateSoin).days %}
                                        <div class="w-2 h-2 rounded-full {{ joursDepuisSoin < 30 ? 'bg-green-400' : (joursDepuisSoin < 90 ? 'bg-yellow-400' : 'bg-red-400') }} mr-2"></div>
                                        <span class="text-xs text-gray-600">{{ joursDepuisSoin }}j</span>
                                    {% else %}
                                        <div class="w-2 h-2 rounded-full bg-gray-400 mr-2"></div>
                                        <span class="text-xs text-gray-400">Aucun</span>
                                    {% endif %}
                                </div>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                                <div class="flex items-center justify-end space-x-2">
                                    <button onclick="showDetails({{ betail.id }})" class="text-indigo-600 hover:text-indigo-900" title="Voir détails">
                                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                                        </svg>
                                    </button>
                                    <button onclick="editAnimal({{ betail.id }})" class="text-gray-600 hover:text-gray-900" title="Modifier">
                                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                                        </svg>
                                    </button>
                                    <div class="relative" x-data="{ open: false }">
                                        <button @click="open = !open" @click.away="open = false" class="text-gray-400 hover:text-gray-600">
                                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v.01M12 12v.01M12 19v.01M12 6a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2z"></path>
                                            </svg>
                                        </button>
                                        <div x-show="open" x-transition class="absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg z-50 border border-gray-200">
                                            <div class="py-1">
                                                <a href="#" class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" onclick="addMedicalRecord({{ betail.id }})">
                                                    <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                                                    </svg>
                                                    Fiche médicale
                                                </a>
                                                <a href="#" class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" onclick="addFeeding({{ betail.id }})">
                                                    <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                                                    </svg>
                                                    Alimentation
                                                </a>
                                                <a href="#" class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" onclick="addReproduction({{ betail.id }})">
                                                    <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
                                                    </svg>
                                                    Reproduction
                                                </a>
                                                <div class="border-t border-gray-200 my-1"></div>
                                                <a href="#" class="flex items-center px-4 py-2 text-sm text-red-700 hover:bg-red-50" onclick="sellAnimal({{ betail.id }})">
                                                    <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
                                                    </svg>
                                                    Vendre
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="8" class="px-6 py-12 text-center text-gray-500">
                                <div class="text-center">
                                    <div class="w-16 h-16 rounded-full flex items-center justify-center text-4xl mx-auto mb-4
                                        {% if type == 'bovin' %}bg-purple-100 text-purple-600
                                        {% elseif type == 'ovin' %}bg-yellow-100 text-yellow-600  
                                        {% elseif type == 'caprin' %}bg-green-100 text-green-600
                                        {% elseif type == 'porcin' %}bg-pink-100 text-pink-600
                                        {% elseif type == 'equin' %}bg-blue-100 text-blue-600
                                        {% else %}bg-gray-100 text-gray-600{% endif %}">
                                        {% if type == 'bovin' %}🐄
                                        {% elseif type == 'ovin' %}🐑
                                        {% elseif type == 'caprin' %}🐐
                                        {% elseif type == 'porcin' %}🐷
                                        {% elseif type == 'equin' %}🐎
                                        {% else %}🐾{% endif %}
                                    </div>
                                    <h3 class="text-lg font-medium text-gray-900 mb-2">Aucun {{ sousTypeName|lower }} enregistré</h3>
                                    <p class="text-gray-500">Commencez par ajouter votre premier {{ sousTypeName|lower }}.</p>
                                    <button class="btn btn-primary mt-4" onclick="addNewAnimal('{{ type }}', '{{ sousType }}')">Ajouter un {{ sousTypeName|lower }}</button>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <!-- Actions sur sélection multiple -->
    <div id="bulk-actions" class="fixed bottom-4 left-1/2 transform -translate-x-1/2 bg-white shadow-lg rounded-lg border border-gray-200 px-6 py-3 hidden">
        <div class="flex items-center space-x-4">
            <span class="text-sm font-medium text-gray-700">
                <span id="selected-count">0</span> animal(aux) sélectionné(s)
            </span>
            <div class="flex space-x-2">
                <button class="btn btn-sm btn-outline" onclick="bulkMove()">
                    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4"></path>
                    </svg>
                    Déplacer
                </button>
                <button class="btn btn-sm btn-outline" onclick="bulkExport()">
                    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                    </svg>
                    Exporter
                </button>
                <button class="btn btn-sm btn-danger" onclick="bulkDelete()">
                    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                    </svg>
                    Supprimer
                </button>
            </div>
            <button onclick="clearSelection()" class="text-gray-400 hover:text-gray-600">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
            </button>
        </div>
    </div>

    <script>
        // Gestion des cases à cocher et actions groupées
        function toggleSelectAll() {
            const selectAll = event.target;
            const checkboxes = document.querySelectorAll('.animal-checkbox');
            checkboxes.forEach(cb => cb.checked = selectAll.checked);
            updateBulkActions();
        }
        
        function updateBulkActions() {
            const selected = document.querySelectorAll('.animal-checkbox:checked');
            const bulkActions = document.getElementById('bulk-actions');
            const selectedCount = document.getElementById('selected-count');
            
            if (selected.length > 0) {
                bulkActions.classList.remove('hidden');
                selectedCount.textContent = selected.length;
            } else {
                bulkActions.classList.add('hidden');
            }
        }
        
        function clearSelection() {
            document.querySelectorAll('.animal-checkbox').forEach(cb => cb.checked = false);
            document.querySelector('thead input[type="checkbox"]').checked = false;
            updateBulkActions();
        }
        
        // Filtrage
        function filterAnimals() {
            const status = document.getElementById('filter-status').value;
            const rows = document.querySelectorAll('.animal-row');
            
            rows.forEach(row => {
                if (!status || row.dataset.status === status) {
                    row.style.display = '';
                } else {
                    row.style.display = 'none';
                }
            });
        }
        
        // Actions individuelles
        function showDetails(id) {
            window.location.href = "{{ path('betail_show', {id: 'PLACEHOLDER'}) }}".replace('PLACEHOLDER', id);
        }
        
        function editAnimal(id) {
            alert('Modifier animal ID: ' + id);
        }
        
        function addMedicalRecord(id) {
            alert('Ajouter fiche médicale pour animal ID: ' + id);
        }
        
        function addFeeding(id) {
            alert('Ajouter alimentation pour animal ID: ' + id);
        }
        
        function addReproduction(id) {
            alert('Ajouter reproduction pour animal ID: ' + id);
        }
        
        function sellAnimal(id) {
            if (confirm('Êtes-vous sûr de vouloir vendre cet animal ?')) {
                alert('Vendre animal ID: ' + id);
            }
        }
        
        function addNewAnimal(type, sousType) {
            // Show modal or redirect to add new animal form with farm context
            alert('Ajouter nouveau animal - Type: ' + type + ', Sous-type: ' + sousType + ', Ferme: {{ fermeActive.id }}');
        }
        
        function importAnimals() {
            alert('Import d\'animaux à implémenter');
        }
        
        // Actions groupées
        function bulkMove() {
            const selected = Array.from(document.querySelectorAll('.animal-checkbox:checked')).map(cb => cb.value);
            alert('Déplacer les animaux: ' + selected.join(', '));
        }
        
        function bulkExport() {
            const selected = Array.from(document.querySelectorAll('.animal-checkbox:checked')).map(cb => cb.value);
            alert('Exporter les animaux: ' + selected.join(', '));
        }
        
        function bulkDelete() {
            const selected = Array.from(document.querySelectorAll('.animal-checkbox:checked')).map(cb => cb.value);
            if (confirm('Êtes-vous sûr de vouloir supprimer ' + selected.length + ' animaux ?')) {
                alert('Supprimer les animaux: ' + selected.join(', '));
            }
        }
        
        // Écouter les changements de cases à cocher
        document.addEventListener('DOMContentLoaded', function() {
            document.querySelectorAll('.animal-checkbox').forEach(cb => {
                cb.addEventListener('change', updateBulkActions);
            });
        });
    </script>
{% endblock %}