{% extends 'base.html.twig' %}

{% block title %}Gestion Financière - {{ parent() }}{% endblock %}

{% block page_title %}Gestion Financière{% endblock %}
{% block page_subtitle %}Suivi des recettes, dépenses et rentabilité{% endblock %}

{% block header_actions %}
    <div class="flex space-x-3">
        <button class="btn btn-primary">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
            Nouvelle Transaction
        </button>
        <button class="btn btn-outline">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
            </svg>
            Rapport Mensuel
        </button>
        <a href="{{ path('finances_rapport') }}" class="btn btn-outline">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
            Rapport Annuel
        </a>
    </div>
{% endblock %}

{% block body %}
    <!-- Indicateurs financiers du mois -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-6">
        <div class="stat-card">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center">
                        <span class="text-xl">💰</span>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-500">Recettes du Mois</p>
                    <p class="text-2xl font-semibold text-green-600">{{ recettesMois|number_format(0, ',', ' ') }}€</p>
                </div>
            </div>
        </div>

        <div class="stat-card">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-10 h-10 bg-red-100 rounded-lg flex items-center justify-center">
                        <span class="text-xl">💸</span>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-500">Dépenses du Mois</p>
                    <p class="text-2xl font-semibold text-red-600">{{ depensesMois|number_format(0, ',', ' ') }}€</p>
                </div>
            </div>
        </div>

        <div class="stat-card">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-10 h-10 {{ beneficeMois >= 0 ? 'bg-green-100' : 'bg-red-100' }} rounded-lg flex items-center justify-center">
                        <span class="text-xl">{{ beneficeMois >= 0 ? '📈' : '📉' }}</span>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-500">Bénéfice du Mois</p>
                    <p class="text-2xl font-semibold {{ beneficeMois >= 0 ? 'text-green-600' : 'text-red-600' }}">
                        {{ beneficeMois|number_format(0, ',', ' ') }}€
                    </p>
                </div>
            </div>
        </div>

        <div class="stat-card">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center">
                        <span class="text-xl">📊</span>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-500">Marge</p>
                    {% set marge = recettesMois > 0 ? ((beneficeMois / recettesMois) * 100) : 0 %}
                    <p class="text-2xl font-semibold {{ marge >= 0 ? 'text-blue-600' : 'text-red-600' }}">
                        {{ marge|number_format(1) }}%
                    </p>
                </div>
            </div>
        </div>
    </div>

    <!-- Graphique d'évolution -->
    {% if evolutionMois|length > 0 %}
        <div class="card mb-6">
            <div class="card-header">
                <h3 class="card-title">Évolution Financière - 6 derniers mois</h3>
            </div>
            <div class="p-6">
                <div class="space-y-4">
                    {% for mois in evolutionMois %}
                        <div class="flex items-center">
                            <div class="w-24 text-sm text-gray-600">
                                {{ mois.mois|date('M Y') }}
                            </div>
                            <div class="flex-1 mx-4">
                                <div class="flex items-center space-x-4">
                                    <!-- Barre recettes -->
                                    <div class="flex-1">
                                        <div class="flex items-center justify-between mb-1">
                                            <span class="text-xs text-green-600">Recettes</span>
                                            <span class="text-xs text-green-600">{{ mois.recettes|number_format(0, ',', ' ') }}€</span>
                                        </div>
                                        <div class="w-full bg-gray-200 rounded-full h-2">
                                            {% set maxMontant = 10000 %}
                                            {% for m in evolutionMois %}
                                                {% if m.recettes > maxMontant %}
                                                    {% set maxMontant = m.recettes %}
                                                {% endif %}
                                            {% endfor %}
                                            <div class="bg-green-500 h-2 rounded-full" 
                                                 style="width: {{ maxMontant > 0 ? (mois.recettes / maxMontant * 100) : 0 }}%"></div>
                                        </div>
                                    </div>
                                    <!-- Barre dépenses -->
                                    <div class="flex-1">
                                        <div class="flex items-center justify-between mb-1">
                                            <span class="text-xs text-red-600">Dépenses</span>
                                            <span class="text-xs text-red-600">{{ mois.depenses|number_format(0, ',', ' ') }}€</span>
                                        </div>
                                        <div class="w-full bg-gray-200 rounded-full h-2">
                                            <div class="bg-red-500 h-2 rounded-full" 
                                                 style="width: {{ maxMontant > 0 ? (mois.depenses / maxMontant * 100) : 0 }}%"></div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="w-20 text-right">
                                {% set benefice = mois.recettes - mois.depenses %}
                                <span class="text-sm font-medium {{ benefice >= 0 ? 'text-green-600' : 'text-red-600' }}">
                                    {{ benefice|number_format(0, ',', ' ') }}€
                                </span>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    {% endif %}

    <!-- Filtres -->
    <div class="card mb-6">
        <div class="p-6">
            <div class="grid grid-cols-1 md:grid-cols-5 gap-4">
                <div>
                    <label for="ferme-filter" class="form-label">Ferme</label>
                    <select id="ferme-filter" class="form-select">
                        <option value="">Toutes les fermes</option>
                        {% for ferme in fermes %}
                            <option value="{{ ferme.id }}">{{ ferme.nom }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div>
                    <label for="type-filter" class="form-label">Type</label>
                    <select id="type-filter" class="form-select">
                        <option value="">Tous</option>
                        <option value="recette">Recettes</option>
                        <option value="depense">Dépenses</option>
                    </select>
                </div>
                <div>
                    <label for="categorie-filter" class="form-label">Catégorie</label>
                    <select id="categorie-filter" class="form-select">
                        <option value="">Toutes</option>
                        <option value="vente_oeufs">Vente œufs</option>
                        <option value="vente_viande">Vente viande</option>
                        <option value="vente_lait">Vente lait</option>
                        <option value="alimentation">Alimentation</option>
                        <option value="veterinaire">Vétérinaire</option>
                        <option value="equipement">Équipement</option>
                    </select>
                </div>
                <div>
                    <label for="date-debut" class="form-label">Date début</label>
                    <input type="date" id="date-debut" class="form-input">
                </div>
                <div>
                    <label for="date-fin" class="form-label">Date fin</label>
                    <input type="date" id="date-fin" class="form-input">
                </div>
            </div>
        </div>
    </div>

    <!-- Liste des transactions -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Dernières Transactions</h3>
        </div>
        
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Type</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Catégorie</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Description</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Ferme</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Montant</th>
                        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    {% for transaction in transactions %}
                        <tr class="hover:bg-gray-50">
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                {{ transaction.dateTransaction|date('d/m/Y') }}
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full
                                    {% if transaction.type == 'recette' %}bg-green-100 text-green-800
                                    {% else %}bg-red-100 text-red-800
                                    {% endif %}">
                                    {% if transaction.type == 'recette' %}
                                        <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.293l-3-3a1 1 0 00-1.414 1.414L10.586 9.5H7a1 1 0 100 2h3.586l-1.293 1.293a1 1 0 101.414 1.414l3-3a1 1 0 000-1.414z"></path>
                                        </svg>
                                    {% else %}
                                        <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm.707-10.293a1 1 0 00-1.414-1.414l-3 3a1 1 0 000 1.414l3 3a1 1 0 001.414-1.414L9.414 11.5H13a1 1 0 100-2H9.414l1.293-1.293z"></path>
                                        </svg>
                                    {% endif %}
                                    {{ transaction.type|title }}
                                </span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                {{ transaction.categorie|replace('_', ' ')|title }}
                            </td>
                            <td class="px-6 py-4 text-sm text-gray-900">
                                {{ transaction.description }}
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                {{ transaction.ferme.nom }}
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                                <span class="{{ transaction.type == 'recette' ? 'text-green-600' : 'text-red-600' }}">
                                    {{ transaction.type == 'recette' ? '+' : '-' }}{{ transaction.montant|number_format(2, ',', ' ') }}€
                                </span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                                <div class="flex items-center justify-end space-x-2">
                                    <button class="text-indigo-600 hover:text-indigo-900" title="Voir détails">
                                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                                        </svg>
                                    </button>
                                    <button class="text-green-600 hover:text-green-900" title="Modifier">
                                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                                        </svg>
                                    </button>
                                    <button class="text-red-600 hover:text-red-900" title="Supprimer">
                                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                                        </svg>
                                    </button>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="7" class="px-6 py-12 text-center text-gray-500">
                                <div class="text-center">
                                    <span class="text-6xl mb-4 block">💰</span>
                                    <h3 class="text-lg font-medium text-gray-900 mb-2">Aucune transaction enregistrée</h3>
                                    <p class="text-gray-500">Commencez par ajouter votre première transaction.</p>
                                    <button class="btn btn-primary mt-4">Ajouter une transaction</button>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}